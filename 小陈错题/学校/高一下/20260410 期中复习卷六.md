## 20260410 期中复习卷（六）

### 一、填空题
1. 已知角 $\alpha$ 是第三象限角，则 $180^\circ - \alpha$ 是第\_\_\_\_\_\_\_\_\_\_\_\_象限角。

2. 已知 $\sin(\pi - \alpha) - 2\cos(-\alpha) = 0$，则 $\tan\alpha =$\_\_\_\_\_\_\_\_\_\_\_\_。

3. 已知点 $A(-3,4)$ 在角 $\alpha$ 的终边上，现将 $\alpha$ 的终边绕坐标原点 $O$ 逆时针旋转 $\dfrac{\pi}{2}$ 后，点 $A$ 运动到点 $B$，则点 $B$ 的坐标是\_\_\_\_\_\_\_\_\_\_\_\_。

4. 设 $\alpha$ 角属于第二象限，且 $\left|\cos\dfrac{\alpha}{2}\right| = -\cos\dfrac{\alpha}{2}$，则角 $\dfrac{\alpha}{2}$ 属于\_\_\_\_\_\_\_\_\_\_\_\_象限。

5. 已知 $\sin\alpha + \cos\alpha = \dfrac{1}{2}$，则 $\tan\alpha + \cot\alpha$ 的值为\_\_\_\_\_\_\_\_\_\_\_\_。

6. 已知扇形的半径为 $R$，面积为 $\dfrac{\sqrt{3}}{2}R^2$，则扇形的圆心角 $\alpha =$\_\_\_\_\_\_\_\_\_\_\_\_。

7. 若 $\dfrac{1 - \tan A}{1 + \tan A} = 4 + \sqrt{5}$，则 $\cot\left(\dfrac{\pi}{4} + A\right) =$\_\_\_\_\_\_\_\_\_\_\_\_。

8. 在 $\triangle ABC$ 中，$a\cos A = b\cos B$，则 $\triangle ABC$ 的形状是\_\_\_\_\_\_\_\_\_\_\_\_三角形。

9. 把 $y = f(x)$ 的图像沿 $x$ 轴向左平移 $\dfrac{\pi}{4}$ 个单位，再把每个点的横坐标伸长到原来的 $2$ 倍（纵坐标保持不变），得到函数 $y = \sin x$ 的图像，则 $y = f(x)$ 的解析式为\_\_\_\_\_\_\_\_\_\_\_\_。

10. 在 $\triangle ABC$ 中，若 $AB = 2$，$AC^2 + BC^2 = 12$，则 $\triangle ABC$ 面积的最大值为\_\_\_\_\_\_\_\_\_\_\_\_。

11. ==若函数 $y = \cos x - \dfrac{m}{2}$，$x \in \left[\dfrac{\pi}{3}, \dfrac{4\pi}{3}\right]$ 有两个零点，则实数 $m$ 的取值范围为\_\_\_\_\_\_\_\_\_\_\_\_==。

12. 如图，已知函数 $y = A\sin(\omega x + \varphi),\ \varphi \in \left(0, \dfrac{\pi}{2}\right)$ 的图像与 $y$ 轴的交点为 $(0,1)$，并已知其在 $y$ 轴右侧的第一个最高点和第一个最低点的坐标分别为 $(x_0, 2)$ 和 $(x_0 + 2\pi, -2)$，记 $y = f(x)$，则 $f\left(\dfrac{\pi}{3}\right) =$\_\_\_\_\_\_\_\_\_\_\_\_。<img src="D:\baibaichen\Blog2\小陈错题\学校\高一下\image\26.png" alt="image-20260411005025327" style="zoom:33%;" />

### 二、选择题
13. 在 $\triangle ABC$ 中，若 $AC = \sqrt{6}$，$C = \dfrac{\pi}{4}$，$AB = 2$，则 $\triangle ABC$ 解的个数为（  ）
A. $0$ 个                                          B. $1$ 个                                     C. $2$ 个                              D. 不确定

14. 化简：$\sqrt{2 + \cos 4 - \sin^2 2}$ 得（  ）
A. $\sin 2$                                       B. $\sqrt{3}\sin 2$                              C. $-\cos 2$                      D. $-\sqrt{3}\cos 2$

15. 函数 $f(x) = \cos\left(2x + \dfrac{\pi}{2}\right)$ 是（  ）
A. 最小正周期为 $2\pi$ 的奇函数                                           B. 最小正周期为 $\pi$ 的奇函数
C. 最小正周期为 $2\pi$ 的偶函数                                           D. 最小正周期为 $\pi$ 的偶函数

16. 将函数 $y = \sin x$ 图像上各点的横坐标缩小为原来的 $\dfrac{1}{4}$，纵坐标不变，再将所得图像向左平移 $\dfrac{\pi}{24}$ 个单位长度得到函数 $y = f(x)$ 的图像，则 $f(x)$ 的解析式为（  ）
    A. $f(x) = \sin\left(\dfrac{1}{4}x + \dfrac{\pi}{24}\right)$                                     B. $f(x) = \sin\left[\dfrac{1}{4}\left(x + \dfrac{\pi}{24}\right)\right]$

    C. $f(x) = \sin\left(4x + \dfrac{\pi}{24}\right)$                                         D. $f(x) = \sin\left[4\left(x + \dfrac{\pi}{24}\right)\right]$

### 三、解答题
17. 已知 $\alpha$、$\beta$ 为锐角，且 $\cos\alpha = \dfrac{1}{7}$，$\cos(\alpha + \beta) = -\dfrac{11}{14}$，求角 $\beta$ 的大小。

18. 在 $\triangle ABC$ 中，内角 $A$，$B$，$C$ 的对边分别为 $a$，$b$，$c$，已知 $a = 2\sqrt{3}$，$(\sin A - \sin B)(b + a) = c(\sin B + \sin C)$，求 $\triangle ABC$ 外接圆的面积。

19. 已知函数 $f(x) = \begin{cases}
\cos x, & -\pi \leq x < 0 \\
\sin x, & 0 \leq x \leq \pi
\end{cases}$
(1) 作出函数 $f(x)$ 的图象，并指出 $f(x)$ 的单调递减区间；     (2) 若 $f(x) = \dfrac{1}{2}$，求 $x$ 的值；
(3) 若关于 $x$ 的方程 $f(x) = a$ 只有唯一解，求实数 $a$ 的取值范围。

20. 已知函数 $f(x) = \cos x(\sin x + \sqrt{3}\cos x) - \dfrac{\sqrt{3}}{2}$，$x \in \mathbb{R}$。
(1) 求函数 $f(x)$ 的单调减区间；
(2) 若存在 $x \in \left[0, \dfrac{\pi}{2}\right]$，使等式 $[f(x)]^2 + f(x) + m = 0$ 成立，求实数 $m$ 的取值范围。

21. 已知函数 $f(x) = \cos\dfrac{x}{3}\left(\sin\dfrac{x}{3} + \sqrt{3}\cos\dfrac{x}{3}\right)$。
(1) 将 $f(x)$ 写成 $A\sin(\omega x + \varphi) + B$（$A > 0, \omega > 0, \varphi \in \left(-\dfrac{\pi}{2}, \dfrac{\pi}{2}\right)$）的形式，并写出其最小正周期，单调递减区间，奇偶性（不要证明）；
(2) 如果 $\triangle ABC$ 的三边 $a$、$b$、$c$ 满足 $b^2 = ac$，且边 $b$ 所对的角为 $x$，试求 $x$ 的范围及此时函数 $f(x)$ 的值域。

---


### 练习 1
已知函数 $f(x)=2\sqrt{3}\sin x\cos x+2\cos^2 x$。
(1) 求 $f(x)$ 严格增区间；
(2) 求 $f(x)$ 在闭区间 $[0,\dfrac{\pi}{3}]$ 上的最大值和最小值及此时 $x$ 的值。

### 练习 2
已知函数 $f(x)=2\sin x\cos x-2\sqrt{3}\cos^2 x+\sqrt{3}$。
(1) 求 $f(x)$ 的最小值，并写出 $x$ 为何值时取得最小值；
(2) 求函数的单调递增区间；
(3) 当 $x\in[\dfrac{\pi}{4},\dfrac{\pi}{2}]$ 时，求函数的值域。

### 练习  3
已知函数 $y=f(x)$ 的表达式为 $f(x)=\sin \omega x$，$\omega>0$。
(1) 若函数 $y=f(x)$ 的最小正周期为 $\dfrac{\pi}{2}$，求 $\omega$ 的值及 $y=f(x)$ 的单调增区间；
(2) 若 $\omega=2$，设函数 $y=g(x)$ 的表达式为 $g(x)=f(x)+\sqrt{3}\cos 2x$，求当 $x\in[0,\dfrac{\pi}{2}]$ 时，$y=g(x)$ 的值域。

### 练习 4
已知函数 $f(x)=4\sin x\cos\left(x+\dfrac{\pi}{3}\right)+\sqrt{3}(x\in \mathbb{R})$。
(1) 将函数形式化简为 $y=A\sin(\omega x+\varphi)+b$ 的形式，写出其振幅、初相与最小正周期；
(2) 将函数 $f(x)$ 的图像向右移动 $\dfrac{\pi}{6}$ 个单位，再将所得图像上各点的横坐标缩短到原来的 $\dfrac{1}{3}$ 得到 $y=g(x)$ 的图像，求 $y=g(x)$ 的解析式。