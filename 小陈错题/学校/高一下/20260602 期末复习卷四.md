## 20260602 期末复习卷（四）

### 一、填空题
1. 化简：$\overrightarrow{AC}-\overrightarrow{BD}+\overrightarrow{CD}-\overrightarrow{AB} =$\_\_\_\_\_\_\_\_\_\_\_。
2. 若 $\sin x = \dfrac12$，$x\in[0,\pi]$，则角 $x=$\_\_\_\_\_\_\_\_\_\_\_。
3. 等比数列 $\{a_n\}(n\in \mathbb{N}^*)$ 中，若 $a_5=\dfrac1{16}$，$a_2=\dfrac12$，则 $a_4=$\_\_\_\_\_\_\_\_\_\_\_。
4. 已知角 $\theta$ 的顶点在坐标原点，始边与 $x$ 轴的正半轴重合，终边经过点 $P(-3,-4)$，则 $\sin\left(\theta+\dfrac\pi2\right)=$\_\_\_\_\_\_\_\_\_\_\_。
5. 已知 $\alpha,\beta$ 为锐角，且 $\sin\alpha=\dfrac45$，$\cos(\alpha+\beta)=\dfrac{12}{13}$，则 $\cos\beta=$\_\_\_\_\_\_\_\_\_\_\_。
6. 已知各项为正的等差数列 $\{a_n\}$，前 $n$ 项和为 $S_n$，若 $a_5+a_7-a_6^2=0$，则 $S_{11}=$\_\_\_\_\_\_\_\_\_\_\_。
7. 计算 $1-i+i^2-i^3+i^4-i^5+\dots-i^{2025}+i^{2026}=$\_\_\_\_\_\_\_\_\_\_\_。
8. 已知向量 $\vec a=(3,4)$，与 $\vec a$ 垂直的单位向量的坐标为\_\_\_\_\_\_\_\_\_\_\_。
9. 设数列 $\{a_n\}$ 的首项 $a_1=1$，前 $n$ 项和为 $S_n$。已知向量 $\vec \alpha=(1,a_n)$，$\vec b=\left(a_{n+1},\dfrac12\right)$ 满足 $\vec \alpha\perp\vec b$，则 $\lim\limits_{n\to\infty}S_n=$\_\_\_\_\_\_\_\_\_\_\_。
10. 已知复数 $z=\dfrac{(1+i)^3(a-i)^2}{\sqrt2(a-3i)^2}(a\in \mathbb{R})$，且 $|z|=\dfrac23$，则 $a=$\_\_\_\_\_\_\_\_\_\_\_。
11. 设 $\sin\alpha,\cos\alpha$ 是关于 $x$ 的方程 $x^2-\dfrac15x+m=0(m\in \mathbb{R})$ 的解，则 $\sin2\alpha=$\_\_\_\_\_\_\_\_\_\_\_。
12. ❌如图，在半径为 $r$ 的圆内作内接正六边形，再作正六边形的内切圆，又在此内切圆内作内接正六边形，如此无限继续下去，设 $S_n$ 为前 $n$ 个圆的面积之和，则 $\lim\limits_{n\to\infty}S_n=$\_\_\_\_\_\_\_\_\_\_\_。<img src="D:\baibaichen\Blog2\小陈错题\学校\高一下\image\46.png" alt="image-20260602201053587" style="zoom:33%;" />

### 二、选择题
13. 下列函数中是偶函数，以 $\pi$ 为最小正周期，且在 $\left(0,\dfrac\pi2\right)$ 上为增函数的是（）
A. $y=\sin x$　                     B. $y=\cos x$　                       C. $y=|\sin x|$　               D. $y=|\cos x|$

14. 设 $\vec a、\vec b$ 为非零向量，则“$\vec a\cdot\vec b>0$”是“$\vec a$ 与 $\vec b$ 的夹角为锐角”的（）
A. 充分不必要条件　                             B. 必要不充分条件
C. 充要条件                                         　D. 既不充分也不必要条件

15. 无穷等差数列 $\{a_n\}$ 的首项为 $a_1$，公差为 $d$，前 $n$ 项和为 $S_n(n\in \mathbb{N}^*)$，则“$a_1+d>0$”是“$\{S_n\}$ 为递增数列”的（）条件
A. 充分非必要　               B. 必要非充分　                   C. 充要　                     D. 既非充分也非必要

16. ❌设 $\{a_n\}$ 为等差数列，其前 $n$ 项和为 $S_n$，若 $(S_8-S_7)(S_9-S_7)<0$，则满足 $S_mS_{m+1}<0$ 的正整数 $m=$（）
A. $12$　                                B. $13$　                                  C. $14$　                           D. $15$

### 三、解答题
17. 设平面上有两个向量 $\vec \alpha=(\cos\alpha,\sin\alpha)(0^\circ\le\alpha<360^\circ)$，$\vec \beta=\left(-\dfrac12,\dfrac{\sqrt3}{2}\right)$。
(1) ❌求证：向量 $\vec \alpha+\vec \beta$ 与 $\vec \alpha-\vec \beta$ 垂直；
(2) 当向量 $\sqrt3\vec \alpha+\vec \beta$ 与 $\vec \alpha-\sqrt3\vec \beta$ 的模相等时，求 $\alpha$ 的大小。

18. 已知 $\tan\dfrac\alpha2=2$。(1) 求 $\tan\left(\alpha+\dfrac\pi4\right)$ 的值；
(2) 求 $\dfrac{\sin\alpha\cos\alpha}{\sin^2\alpha+\sin2\alpha+\cos2\alpha+1}$ 的值。

19. 等差数列 $\{a_n\}$，等比数列 $\{b_n\}$，$a_n=20-n$，$n\in \mathbb{N}^*$，如果 $b_1=a_{18}$，$b_4=a_4$。
(1) 求 $\{b_n\}$ 的通项公式；
(2) $c_n=a_n+b_n$，求 $\{c_n\}$ 的前 $n$ 项和 $S_n$ 的值。

20. ❌已知数列 $\{a_n\}$ 的前 $n$ 项和 $S_n$ 满足 $\sqrt{S_{n+1}}=\sqrt{S_n}+2(n\in \mathbb{N}^*)$，且 $a_1=4$。
(1) 求数列 $\{a_n\}$ 的通项公式 $a_n$；
(2) 记 $b_n=\dfrac1{a_na_{n+1}}$，求数列 $\{b_n\}$ 的前 $n$ 项和 $T_n$。

21. (1) 求函数 $f(x)=2\sqrt3\sin^2\left(x+\dfrac\pi4\right)+2\cos^2x-\sqrt3$ 的最小正周期；
(2) ❌在 $\triangle ABC$ 中，$\triangle ABC$ 的面积为 $S$，且 $\overrightarrow{AB}\cdot\overrightarrow{AC}=8$，$4\le S\le4\sqrt3$。求 $f(A)$ 的取值范围。